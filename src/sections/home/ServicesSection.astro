---
import { Image } from "@astrojs/image/components";
import { SectionsLayout } from "@Layouts";
import { BaseText, BaseTextTypes } from "@BaseComponents";
import { SitesPC } from '@Assets';

---
<SectionsLayout class="services">
  <div class="container left-image-section">
    <div class="services-images">
      <Image
          alt="Imagem de vários tipos de dispositvos com sites personalizados."
          class="image"
          title="Sites Personalizados"
          src={SitesPC}
        />
    </div>

    <div class="services-descriptions">
      <BaseText
        tag={BaseTextTypes.TAGS.H2}
        class="service-title"
        variation={BaseTextTypes.VARIATIONS.GRADIENT}
      >
        Desenvolvimento de Sites Personalizados
      </BaseText>

      <BaseText
        tag={BaseTextTypes.TAGS.P}
        class="service-description"
        variation={BaseTextTypes.VARIATIONS.PARAGRAPH}
      >
        Nossos sites são feitos sob medida para atender às necessidades
        específicas de cada cliente. Utilizamos tecnologias e ferramentas avançadas,
        como AstroJs, VueJs e WordPress, para criar experiências online
        impressionantes e funcionais.
      </BaseText>

    </div>
  </div>
</SectionsLayout>

<style scoped>
.services {
  @apply bg-dark-blue
}
.left-image-section {
  @apply flex flex-col lg:flex-row items-center justify-center
}

.services-images {
  @apply w-max lg:w-1/3
}

.services-descriptions {
  @apply w-full lg:w-2/3
}

.image {
  @apply max-w-md w-full
}

.service-title {
  @apply text-center lg:text-left
}

.service-description {
  @apply mt-8 text-center lg:text-left
}
</style>